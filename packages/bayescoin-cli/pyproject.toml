[project]
name = "bayescoin-cli"
version = "0.1.3"
description = ""
readme = "README.md"
authors = [
    { name = "oyghen", email = "oyghen@duck.com" }
]
requires-python = ">=3.11"
dependencies = [
    "bayescoin==0.1.3",
    "typer>=0.24.1",
]

[tool.uv.sources]
bayescoin = { workspace = true }

[project.scripts]
bayescoin = "bayescoin_cli.cli:app"

[build-system]
requires = ["uv_build>=0.11.7,<0.12.0"]
build-backend = "uv_build"
